    import XCTest
    @testable import Mutanus

    final class MutanusTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Mutanus().text, "Hello, World!")
        }
    }
